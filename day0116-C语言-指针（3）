七.函数指针变量（  int (*p)(int,int)   ）
1.对于函数，函数名和&函数名都是函数的地址
2.函数指针的使用
int Add(int x,int y)
{
    return x+y;
}
int main()
{
    int (*p)(int,int)=&Add;
    int ret=(*p)(3,5);//也可写成：p(3,5)或(******p)(3,5)，
    //只需拿到Add的地址即可
    //因为原先调用函数时，函数名代表的就是它的地址，通过地址调用函数
    cout<<ret;
    return 0;
}

(*(void(*)())0)();
//第一个 * 可省略
//调用0地址处的函数，调用函数的参数是无参，返回类型是void
