十.回调函数
1.定义：通过函数指针调用的函数

十一.C/C++语言中的指针类型隐式转换
1.C中
（1）在C/C++中，指针类型一般只能隐式类型转换为void*（该类型指针无法++/--，因为无具体类型，所以不知道一次 加/减 几个字节），其余类型只能显式转换
（2）C/C++中，结构体嵌套时，子结构体指针可以隐式转换为包含它的父结构体指针
（3）C/C++中非常量指针可以隐式转换为常量指针，但常量指针不能隐式转换为非常量指针（如果支持常量指针隐式转换为非常量指针，会对数据进行修改，违反了const的数值不能修改的规则）
2.C++中
（1）派生类指针可以隐式转换为基类指针（多态）

十二.强制类型转换
1.强制类型转换的优先级低于 . 和 -> ，所以需要使用（）
return ((struct Stu*)e1)->age - ((struct Stu*)e2)->age;
