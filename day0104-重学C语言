1.%5d是右对齐。如果输出数不足五位，左边补0；如果输出数刚好为5位或者超过5位，则正常输出
2.printf()参数与占位符是一一对应关系，如果有n个占位符，printf()的参数就应该有n+1个
3.scanf()处理数值占位符时，会自动过滤空白字符，包括空格、制表符、换行符，scanf输出字符占位符的时候，不忽略空白字符，总是返回当前第一个字符，无论该字符是否为空格。
#include <stdio.h>

int main()
{
    char c = 0;
    scanf("%c", &c);
    printf("%c\n", c);
    %c不会跳过前置的空白字符，而其他的占位符则会跳过前置的空白字符
    return 0;
}
4.2个字符串使用 == 判断时，比较的是起始地址。
5.比较两个字符串是否相等，使用strcmp
6.&&短路的时候，&&的左边操作数为假，右边不再计算
7.数组名既可以表示数组的地址，也可以表示数组首元素的地址。注意：数组名只有在sizeof和&后才代表整个数组，其它都表示首元素的地址
8.随着数组下标的由小到大，地址由高到低
9.变长数组的大小在运行时确定，比如使用变量来指定数组大小，一旦确定大小后，它的大小是固定的，无法改变。
10.函数设计应该追求高内聚低耦合。（高内聚：功能单一；低耦合：每个函数都有明确且独立的功能，它们之间没有相互依赖或依赖程度很低。
11.函数调用原则：先声明后使用
12.定义也是一种特殊的声明
13.static是C语言中的关键字，可以用来修饰局部变量，全局变量，修饰函数
14.extern是C语言中的关键字，用来声明外部符号
